[package]
name = "qwen_tokenizer"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
ahash = "0.8"
tokenizers = { version = "0.21", default-features = false, features = ["onig"] }
serde_json = "1"
jni = "0.21"
flate2 = "1"
unicode-normalization = "0.1"
